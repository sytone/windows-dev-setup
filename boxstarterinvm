# Configure Windows
Set-WindowsExplorerOptions -EnableShowHiddenFilesFoldersDrives -EnableShowProtectedOSFiles -EnableShowFileExtensions -EnableShowFullPathInTitleBar
Disable-BingSearch
Disable-GameBarTips
Enable-RemoteDesktop

Update-ExecutionPolicy Unrestricted
choco feature enable -n allowGlobalConfirmation

## Git
cup git.install
cup poshgit
cup Git-Credential-Manager-for-Windows
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")



## Python
cup python2
if (Test-PendingReboot) { Invoke-Reboot }
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

## Node, npm
cup nodejs.install
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

## NPM Update
npm install -g npm-windows-upgrade
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

## Yarn installation
cup yarn
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

## dot net
cup dotnetcore
cup dotnetcore-runtime
cup dotnetcore-sdk
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

## Editors
cup visualstudiocode
cup vscode-powershell
cup vscode-gitlens
cup vscode-gitattributes
cup vscode-gitignore
cup vscode-editorconfig
cup vscode-csharp
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

## Visual Studio 2017
cup visualstudio2017community
cup visualstudio2017-workload-netcoretools
cup visualstudio2017-workload-vctools
if (Test-PendingReboot) { Invoke-Reboot }

## Basics
cup GoogleChrome
cup 7zip.install
cup sysinternals
if (Test-PendingReboot) { Invoke-Reboot }

# Pinning Things
Install-ChocolateyPinnedTaskBarItem "$env:programfiles\Google\Chrome\Application\chrome.exe"

# Let's get Updates, too
Install-WindowsUpdate -acceptEula
